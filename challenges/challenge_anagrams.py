def is_anagram(first_string, second_string):
    # """Faça o código aqui."""
    raise NotImplementedError
    # first_list = sorted(list(first_string))
    # second_list = sorted(list(second_string))

    # index = 0
    # match = True

    # while index < len(first_string) and match:
    #     if first_list[index] == second_list[index]:
    #         index += 1
    #     else:
    #         match = False

    # return (first_list, second_list, match)

# Referencia
# /https://panda.ime.usp.br/panda/static/
# pythonds_pt/02-AnaliseDeAlgoritmos/ExemploDeDeteccaoDeAnagramas.html
